services:
  - type: static_site
    name: leaveease
    buildCommand: "npm install && npm run build"
    publishPath: "./build"
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
